<?php
// Khởi động session
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}

// Authentication logic removed as per request
// System is now open access without login requirements

